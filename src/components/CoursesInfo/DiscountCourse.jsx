import React from "react";

const DiscountCourse = ({ discount }) => {
  return (
    <>
      <div
        className="w-[200px] rounded-r-xl mt-4 py-[4px] px-4 text-white font-bold"
        style={{ background: color, background: gradient }}
      >
        {discount}
      </div>
    </>
  );
};

export default DiscountCourse;
